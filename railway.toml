[
build
]
builder = "nixpacks"
buildCommand = "yarn build"

[
deploy
]
startCommand = "yarn start"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"

[
variables
]
NODE_VERSION = "20.x"